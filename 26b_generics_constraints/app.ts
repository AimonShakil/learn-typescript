let score : Array <number> = []
const names : Array <string> = []

function indentityFour <T> (val :T) :T { // Advantage of Generics is we can define our own types and can pass into 
    return val
}

// Example
interface Bootle {
    brand : "string",
    type : number
}

// indentityFour <Bootle> ({}) 